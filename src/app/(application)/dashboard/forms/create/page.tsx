"use server"

import FormBuilder from "../formComponents/FormBuilder"

export default async function FormBuilderPage() {
  //maybe I do some auth session thing here? idk how this works

  return <FormBuilder/>
}
